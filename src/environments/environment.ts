export const environment = {
  // Base URL for API requests. Defaults to local backend when not provided via NG_APP_API_BASE_URL.
  apiBaseUrl: import.meta.env?.NG_APP_API_BASE_URL || 'https://thecityofjoy.in/cs_backend/api',
};
